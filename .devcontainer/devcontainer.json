
{
	"name": "Ruby 3",
	"dockerFile": "Dockerfile",

	"customizations": {
		"vscode": {
			"settings": { 
				"terminal.integrated.shell.linux": "/bin/bash"
			}
		}
	},

	"postCreateCommand": "bundle install ; jekyll serve  --port 4001"

}
